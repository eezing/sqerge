{
  "name": "sqerge",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "bin": "bin.js",
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc --build --clean && tsc --build",
    "test": "prettier -c ."
  },
  "prettier": {
    "bracketSpacing": true,
    "semi": true,
    "singleQuote": true
  },
  "devDependencies": {
    "@types/node": "^16.11.59",
    "prettier": "^2.7.1",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "commander": "^9.4.0"
  }
}
